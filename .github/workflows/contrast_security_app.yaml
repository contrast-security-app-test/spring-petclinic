# DISCLAIMER: This workflow file has been auto-generated and committed to the repo by the GitHub App from Contrast Security.
# Manual edits to this file could cause the integration to produce unexpected behavior or break.
# VERSION 0.0.1
name: Contrast Security App Workflow
on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main
jobs:
  run-contrast-security-workflow:
    uses: 'Contrast-Security-Inc/contrast-security-app-workflow/.github/workflows/contrast_security_app.yml@develop'
    with:
      REPOSITORY_URL: ${{ github.server_url }}/${{ github.repository }}
      REPOSITORY_NAME: ${{ github.repository }}
      CONTRAST_GITHUB_APP_ORG_ID: ${{ vars.CONTRAST_GITHUB_APP_ORG_ID }}
      CONTRAST_GITHUB_APP_TS_URL: ${{ vars.CONTRAST_GITHUB_APP_TS_URL }}
      CONTRAST_GITHUB_APP_ID: ${{ vars.CONTRAST_GITHUB_APP_ID }}
    secrets:
      CONTRAST_GITHUB_APP_API_KEY: ${{ secrets.CONTRAST_GITHUB_APP_API_KEY }}
      CONTRAST_GITHUB_APP_AUTH_HEADER: ${{ secrets.CONTRAST_GITHUB_APP_AUTH_HEADER }}
      ART_API_KEY: ${{ secrets.DOUG_ART_API_KEY }}
